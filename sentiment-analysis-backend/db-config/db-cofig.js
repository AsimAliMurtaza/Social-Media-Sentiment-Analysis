// Database configuration
const config = {
    server: "DESKTOP-UIFCNNO",
    database: "northwind",
    driver: "msnodesqlv8",
    options: {
      trustedConnection: true,
    },
  };

export default config;